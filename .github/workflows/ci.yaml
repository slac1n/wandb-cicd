name: GitHub Actions Demo
on: [push]
jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: |
          echo "Hello World"
      - name: this will fail
        run: exit 1;
    
